# Use below commands to run separate dynos
# clock: babel-node clock.js --presets latest
# worker: babel-node worker.js --presets latest

# Use below command to run on single dyno
worker: npm run start-all